## Packages
framer-motion | For smooth wizard transitions and page entry animations
date-fns | For formatting dates in records
clsx | For conditional class names
tailwind-merge | For merging tailwind classes safely

## Notes
- Tailwind config should extend colors for an agricultural theme (greens, earth tones).
- Fonts: 'Inter' for UI, 'Lora' or 'Merriweather' for headings to give a regulatory/document feel.
- Auth flow uses Replit Auth (blueprint:javascript_log_in_with_replit).
- Wizard logic handles FSMA exemption status determination locally before saving to backend.
